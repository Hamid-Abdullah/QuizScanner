# ── Quiz Scanner — Python Requirements ──────────────────────────────────────
# Install: pip install -r requirements.txt

# Web framework
flask>=3.0.0
werkzeug>=3.0.0

# Image processing (core)
opencv-python>=4.9.0
numpy>=1.26.0
Pillow>=10.2.0

# QR code reading
pyzbar>=0.1.9         # fast ZBar-based decoder (needs libzbar: apt-get install libzbar0)

# QR code generation (for sample generator)
qrcode[pil]>=7.4.2

# OCR engines
easyocr>=1.7.1        # deep learning OCR — handles handwriting well
pytesseract>=0.3.10   # Tesseract OCR fallback (needs tesseract-ocr installed)

# PDF support
PyMuPDF>=1.24.0       # fitz — convert PDF pages to images

# Data / reporting (Task 5)
pandas>=2.2.0
openpyxl>=3.1.2
xlsxwriter>=3.1.9

# Utilities
scikit-image>=0.22.0  # optional: image quality metrics
requests>=2.31.0
python-dotenv>=1.0.0

# ── System dependencies (not pip) ────────────────────────────────────────────
# Ubuntu/Debian:
#   sudo apt-get install -y libzbar0 tesseract-ocr tesseract-ocr-eng libgl1
#
# macOS:
#   brew install zbar tesseract
#
# Windows:
#   Install ZBar from https://sourceforge.net/projects/zbar/
#   Install Tesseract from https://github.com/UB-Mannheim/tesseract/wiki
